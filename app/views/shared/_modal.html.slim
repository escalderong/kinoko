/ daisyUI modal wrapper. Pass `id` and `title`; yield the form/fields as the body.
dialog id=id class="modal"
  div class="modal-box"
    h3 class="font-bold text-lg mb-4" = title
    = yield
  form method="dialog" class="modal-backdrop"
    button = t("app.modal.close")
